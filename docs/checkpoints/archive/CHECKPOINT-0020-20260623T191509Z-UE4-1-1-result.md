# CHECKPOINT 0020: UE-4.1.1 Result (Acceptance Hardening)

## Status: passed

## Execution Context
- UTC Time: 2026-06-23T19:15:09Z
- Workspace: D:\Users\l3d\Documents\AVBOIT\MaterialShaderExample_AVBOIT
- Build: Win64 Development Editor (-game Headless)

## Phase Objectives
1. Perform the explicit UE-4.1.1 Core Math Acceptance Hardening.
2. Freeze the Depth and Slice convention, asserting that `Slice 0` is strictly the physical Near Plane.
3. Correct the memory layout `struct FInjectedFragment` bugs that silently dropped fragments.
4. Convert logging tests into explicit executable tests with exit codes.
5. Successfully pass 9/9 test cases covering `Single`, `Double`, and `Triple` (6 permutations) permutations with color AND transmittance validation.

## Technical Resolutions
1. **Depth Mapping Fixed**: Inverted the logic from The Forge. We now strictly map `ZNear` to `0.0` (Slice 0) and `ZFar` to `1.0` (Slice 63). Transmittance is mathematically correctly integrated from Near to Far, making `FrontTransmittance` physically occlude the target fragment correctly.
2. **Padding Fixed**: Synchronized `FAVBOITInjectedFragment` to be exactly 48 bytes by appending `FIntPoint Padding`, resolving the HLSL vs C++ structure mismatch that silently discarded all but the first fragment.
3. **Array Readback Fixed**: Avoided the UE engine crash with `Texture2DArray` readbacks by embedding the `TotalTransmittance` into the `OutResultTexture.a` channel in `AVBOITComposite.usf`, avoiding out-of-bounds slice locks.
4. **Harness Rewritten**: Automated suite now iterates through 9 mathematically rigorous test cases, computes exact expected vectors, reports standard deviations/max diffs, and strictly calls `RequestExitWithStatus(false, FailedCaseCount > 0 ? 1 : 0)` for CI compatibility.

## Outcomes
- **PASSED**: 9 / 9.
- OIT Order Independence is 100% numerically verified under the correct physical depth mapping.
- Core rendering math stage is now fully accepted and safely frozen.

## Next Steps
Proceed to **UE-4.2**: Mesh Rasterization Path Integration.
