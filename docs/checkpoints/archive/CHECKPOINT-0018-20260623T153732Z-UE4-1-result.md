# Checkpoint 0018: UE-4.1 Result (AVBOIT Core Math Implementation)

## Status: passed

## Execution Context
- UTC Time: 2026-06-23T15:37:32Z
- Workspace: D:\Users\l3d\Documents\AVBOIT\MaterialShaderExample_AVBOIT
- Build: Win64 Development Editor

## Phase Objectives
1. Implement the formal AVBOIT vertical slice (Clear, Build, Integrate, Composite, Shade) inside the plugin using RDG, completely decoupled from Engine Rendering.
2. Develop a standalone test harness (`FAVBOITBackendTestAutomation`) that runs headlessly (`-game`) to validate the mathematical correctness of OIT accumulation via CPU readback.
3. Validate Single (W), Double (R+B), and Triple (R+G+B) permutations to demonstrate convergence order-independence.

## Outcomes
- Fixed missing shader directory mappings by ensuring shaders use the correct virtual path `Plugin/MaterialShaderExample`.
- Solved GPU fence starvation / Engine assertion issues in `-game` headless mode by decoupling asynchronous readback logic (`IsReady()`) and transitioning to `FlushRenderingCommands()` to synchronously lock the RHI Thread, ensuring `Lock()` succeeds and the automation accurately reads the ROI.
- Render results perfectly matched the expected CPU numerical model across all 6 permutations (RGB, RBG, BRG, BGR, GRB, GBR), demonstrating robust Order-Independent Transparency correctly reconstructing the layers.

## Next Steps
Proceeding to the next stages. The core math has been stabilized!
